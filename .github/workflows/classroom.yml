name: Autograding Tests
'on':
- push
- workflow_dispatch
- repository_dispatch
permissions:
  checks: write
  actions: read
  contents: read
jobs:
  run-autograding-tests:
    runs-on: ubuntu-latest
    if: github.actor != 'github-classroom[bot]'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: test treecoords()
      id: test-treecoords
      uses: education/autograding-command-grader@v1
      with:
        test-name: test treecoords()
        setup-command: sudo -H pip3 install pytest
        command: pytest .autograde/grade.py
        timeout: 11
    - name: Autograding Reporter
      uses: education/autograding-grading-reporter@v1
      env:
        TEST-TREECOORDS_RESULTS: "${{steps.test-treecoords.outputs.result}}"
      with:
        runners: test-treecoords
